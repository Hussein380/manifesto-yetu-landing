# Manifesto Yetu

React + Vite + TypeScript + Tailwind + shadcn/ui.

## Run locally
```sh
npm install
npm run dev
```

## Build for production
```sh
npm run build
```

## Deploy on Vercel
Connect the GitHub repo to Vercel and deploy with the default Vite settings (`npm run build`). Public assets live in `public/`.
